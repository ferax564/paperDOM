import {DatabaseSync} from 'node:sqlite';
import {readFileSync} from 'node:fs';
import {collaborationAPI} from '../../server/collaboration.ts';
export function collaborationHarness(){const sql=new DatabaseSync(':memory:');sql.exec(readFileSync(new URL('../../drizzle/0000_white_devos.sql',import.meta.url),'utf8'));const files=new Map();const env={DB:{prepare(query){return{bind(...params){const statement=sql.prepare(query);return{async first(){return statement.get(...params)??null;},async all(){return{results:statement.all(...params)};},async run(){const r=statement.run(...params);return{meta:{changes:Number(r.changes)}};}};}};}},BUCKET:{async put(key,text){files.set(key,text);},async get(key){return files.has(key)?{text:async()=>files.get(key)}:null;},async delete(key){files.delete(key);}}};const call=async(path='',method='GET',body,user='owner',headers={})=>{const response=await collaborationAPI(new Request(`https://test.example/api/decks${path}`,{method,headers:{...(user?{'oai-authenticated-user-id':user,'oai-authenticated-user-email':`${user}@example.com`}:{}),...headers},body:body?JSON.stringify(body):undefined}),env);return{status:response.status,...await response.json()};};return{call,sql,files,env};}
